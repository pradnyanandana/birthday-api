{
  "name": "birthday-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --legacy-watch index.js --exec babel-node",
    "build": "babel . -d dist --presets es2015,stage-2 --ignore spec.js,node_modules",
    "start_babel": "babel-node ./index.js --preset=babel-preset-es2015",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint-js": "npx eslint --fix --ext .js . || echo \"Formatting done! Please fix the remaining warnings and errors\"",
    "migrate": "cd database && migrate-mongo up"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/node": "^7.22.1",
    "@babel/preset-env": "^7.22.4",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.1.3",
    "express": "^4.18.2",
    "joi": "^17.9.2",
    "mongoose": "^7.2.2",
    "nodemon": "^2.0.22",
    "winston": "^3.9.0"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "migrate-mongo": "^10.0.0"
  }
}
